export function InitialSetupWizard({ onComplete }: { onComplete: () => void }) {
  return (
    <div className="max-w-2xl mx-auto p-8">
      <h2 className="text-2xl font-bold mb-6">Configuration initiale</h2>
      {/* Ajouter étapes de configuration */}
    </div>
  );
}